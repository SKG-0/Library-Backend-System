<h2 class="page-header">Recently added</h2>
<div>
    <% books.forEach(book=> { %>
        <a  href="/books/<%= book.id %>"><img class="image-links" src="<%= book.coverImagePath %>" width="100" height="150" alt=""></a>
    <% }) %> 
</div>